function avgData = avgMaps(statsobj, b_useMsk, b_isPolar)
% AVGMAPS creates an average map (i.e. image with dimensions Y,X) per group
% per acquisition.

disp('creating average plots')
% Separate data by groups and aquisitions.

% Perform translation and scaling.

% average maps

% use mask to hide pixels not from ROI.








end

